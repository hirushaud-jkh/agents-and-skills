# VS Code — settings may contain tokens; MCP config uses ${input:} so it's safe to commit
.vscode/settings.json
.vscode/*.local.json

# Claude Code — local settings that may contain tokens
.claude/settings.local.json

# Environment files
.env
.env.*
!.env.example

# Node
node_modules/

# Skill assets — user-provided, not committed
.claude/skills/deck/templates/*.pptx
.github/skills/*/templates/*.pptx
